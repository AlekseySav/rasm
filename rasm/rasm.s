.macro const _Name = _Value
    .macro _Name
        _Value
    .end _Name
.end const

const __rasm__ = 1

.macro macro name (args->((a, b, c))) { val })))
    .macro name args
        val
    .end name
.end macro
